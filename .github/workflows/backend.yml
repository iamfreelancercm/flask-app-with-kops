name: backend

on:
    workflow_run:
      workflows: 
        - "unittest"
      types: 
        - completed
      branches: 
        - anup

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        run: echo "hello github action"  
